
import { needsAnswers } from 'questions-answers/helpers/needs-answers';
import { module, test } from 'qunit';

module('Unit | Helper | needs answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = needsAnswers([42]);
  assert.ok(result);
});

